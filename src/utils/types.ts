export type filtersType = {
  sepia: number;
  grayscale: number;
  hueRotate: number;
  invert: number;
  brightness: number;
  contrast: number;
};

export type filterValues = {
  style: string;
  value: number;
};
